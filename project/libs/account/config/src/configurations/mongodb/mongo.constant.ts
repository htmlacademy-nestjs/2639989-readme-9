export const MIN_PORT = 0;
export const MAX_PORT = 65535;
export const DEFAULT_MONGO_PORT = 27017;

export const EnvValidationMessage = {
  DBHostRequired: 'MongoDB хост необходим',
  DBNameRequired: 'Имя базы данных необходимо',
  DBPortRequired: 'MongoDB порт необходим',
  DBUserRequired: 'MongoDB пользователь необходим',
  DBPasswordRequired: 'MongoDB пароль необходим',
  DBBaseAuthRequired: 'MongoDB база для авторизации необходима',
}
